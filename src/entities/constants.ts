enum FragmentTypes {
  function = "function",
  event = "event",
  constructor = "constructor",
}

enum FunctionTypes {
  payable = "payable",
  nonpayable = "nonpayable",
  pure = "pure",
  view = "view"
}

export {FragmentTypes, FunctionTypes};
